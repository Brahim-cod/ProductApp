@page "/logout"
@inject Shered.Services.IUserService userService
@inject NavigationManager NavigationManager

@code {
    protected override async Task OnInitializedAsync()
    {
        await userService.Logout();
        NavigationManager.NavigateTo("/");
    }
}
